site:
  name: lock4it
  site: 'https://www-lock4it.netlify.app/'
  base: '/'
  trailingSlash: false

  googleSiteVerificationId: a-cRvY11tUe6iGg2IO_RPvnA1q95iEM3M

# Default SEO metadata
metadata:
  title:
    default: lock4it
    template: '%s — AstroWind'
  description: '🔐 lock4it Landing Page.'
  robots:
    index: true
    follow: true
  openGraph:
    site_name: lock4it
    images:
      - url: '~/assets/images/default.png'
        width: 1200
        height: 628
    type: website
  twitter:
    handle: '@onwidget'
    site: '@onwidget'
    cardType: summary_large_image

i18n:
  language: en
  textDirection: ltr

apps:

analytics:
  vendors:
    googleAnalytics:
      id: null # or "G-XXXXXXXXXX"

ui:
  theme: 'system' # Values: "system" | "light" | "dark" | "light:only" | "dark:only"
